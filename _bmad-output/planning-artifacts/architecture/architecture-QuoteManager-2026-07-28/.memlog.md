---
topic: Service Request and Quote Manager
scope: Full system - .NET API, React UI, persistence, messaging seam, audit, auth
purpose: build-substrate
altitude: feature
updated: 2026-07-28T10:24
---

- (direction) Target role is Senior Full Stack Software Engineer reporting to a DevOps Manager. Reviewers will weight working, observable, production-minded code above architecture prose. The spine is a build substrate first and an interview artifact second.
- (decision) Fast path chosen over the coaching path: the load-bearing stack and constraints were already settled with the user in conversation, and the deadline is 48 hours including rehearsal.
- (constraint) Two calendar days elapsed, including rehearsal. Must demo live AND run from a fresh clone on a reviewer machine that is not the authors.
- (constraint) Azure subscription is not currently active and activation carries card-verification risk plus setup cost. Azure integration must be code-present and configuration-gated, never on the runtime critical path.
- (constraint) Local toolchain verified on 2026-07-28: .NET SDK 10.0.302 and 9.0.302 present, Node v22.18.0, Docker 29.6.2, SQL Server LocalDB MSSQLLocalDB present, uv 0.8.2 present, Azure CLI absent.
- (constraint) Global npm on PATH is 8.3.2, installed under AppData/Roaming/npm and shadowing the 10.9.3 bundled with Node 22. Project installs invoke the bundled npm-cli.js by absolute path rather than mutating the users global install.
- (version) .NET 10 is the active LTS: runtime 10.0.10, SDK 10.0.302, ASP.NET Core 10.0.10, EF Core 10.0.10, supported through 2028-11-14. Verified against the dotnet support policy page dated 2026-07-14.
- (version) Classic Application Insights SDK is in maintenance mode. Microsoft currently recommends the Azure Monitor OpenTelemetry Distro for new ASP.NET Core apps: Azure.Monitor.OpenTelemetry.AspNetCore 1.6.0. Verified against Microsoft Learn.
- (version) NuGet stable versions confirmed via the NuGet flat-container API on 2026-07-28: Azure.Messaging.ServiceBus 7.20.2, Serilog.AspNetCore 10.0.0, Serilog.Sinks.Console 6.1.1, Serilog.Sinks.File 7.0.0, Microsoft.AspNetCore.Authentication.JwtBearer 10.0.10, Microsoft.AspNetCore.Identity.EntityFrameworkCore 10.0.10, Microsoft.AspNetCore.OpenApi 10.0.10, Scalar.AspNetCore 2.16.16, FluentValidation 12.1.1, xunit.v3 3.2.2, Microsoft.NET.Test.Sdk 18.8.1, Microsoft.AspNetCore.Mvc.Testing 10.0.10, Shouldly 4.3.0, NSubstitute 6.0.0.
- (version) npm registry versions confirmed on 2026-07-28: react and react-dom 19.2.8, react-router 8.3.0, vite 8.1.5 (previous tag 7.3.6), typescript latest 7.0.2 and latest 6.x is 6.0.3, mantine 9.5.0, tanstack react-query 5.101.4, vitejs plugin-react 6.0.4, playwright 1.62.0, eslint 10.8.0, typescript-eslint 8.65.0.
- (decision) TypeScript pinned to 6.0.3, not the newer 7.0.2. TypeScript 7.0 went GA 2026-07-08 as a native Go port but ships without a stable programmatic API until 7.1, which breaks typescript-eslint parsing and type-aware rules unless a compatibility shim is added. No functional payoff on a small two-day frontend. Revisit when 7.1 ships the API.
- (decision) Swashbuckle is not used. OpenAPI documents come from the in-box Microsoft.AspNetCore.OpenApi 10.0.10 and the interactive UI from Scalar.AspNetCore 2.16.16, which is the current .NET template direction.
- (decision) Assertion library is Shouldly 4.3.0, not FluentAssertions. FluentAssertions 8 and later require a paid commercial licence, which is inappropriate to place in a repository handed to a third party for evaluation.
- (decision) Runtime does NOT depend on Docker. Docker Desktop on Windows adds start-up latency and WSL2 failure modes that are unacceptable on a live demo critical path. A Dockerfile and compose file ship as reviewable artifacts that the demo never executes.
- (decision) Persistence is SQLite via EF Core, single provider, migrations committed and applied on start-up with idempotent seeding. Chosen over SQL Server LocalDB because the deliverable must run from a fresh clone on a reviewer machine. A second provider is explicitly NOT configured: EF migrations are provider-specific and maintaining two sets is not affordable in the timebox.
- (decision) Sibling-quote rejection on accept is SYNCHRONOUS and inside the same transaction, reversing an earlier suggestion to do it asynchronously via the queue. Request is the aggregate root that owns Quotes, so the at-most-one-accepted rule is a single consistency boundary. Making it async would open a window where two quotes both look actionable in the UI.
- (decision) The queue carries integration events for effects OUTSIDE the consistency boundary only - notification dispatch and future external system sync. Invariant maintenance and audit are transactional and never queued.
- (decision) Audit is a transactional projection of domain events written in the same transaction as the state change, not a logging concern. Serilog output is diagnostics and is explicitly not the audit source of truth. This distinction directly answers the users stated intent of logging for tracking client actions, which is actually an audit requirement.
- (decision) Transactional outbox is the sole publish path. A SaveChanges interceptor writes domain events to an Outbox table in the same transaction; a single dispatcher BackgroundService drains it. Delivery is at-least-once so every consumer must be idempotent on the outbox message id.
- (decision) Every outbound dependency sits behind a port with exactly two adapters - a local default requiring no cloud, and an Azure adapter activated only when its connection string is present. Absence of a connection string is a supported configuration, never a start-up exception. Both adapters are held to the same contract tests.
- (decision) Telemetry is OpenTelemetry-native. The local exporter needs no Azure resource; Azure Monitor is one exporter behind the same instrumentation. This makes the config-gated Azure story structural rather than bolted on.
- (decision) Quote lifecycle lives in one declarative transition table in the Domain project and is the sole authority. The API exposes a single action-driven transition endpoint rather than per-status endpoints.
- (decision) The API projects the currently permitted action set onto every quote DTO so the React client renders controls from server truth. The client encodes no lifecycle rules. This closes the most likely divergence between the UI and API halves of the build.
- (decision) Errors cross the boundary as RFC 9457 problem details carrying a stable machine-readable code plus the OpenTelemetry trace id. The UI branches on code, never on human-readable detail text.
- (decision) Authentication is stateless JWT bearer, single scheme, HS256, with a deny-by-default fallback authorization policy so a new endpoint is protected unless deliberately opened. Identity primitives come from AddIdentityCore and PasswordHasher rather than hand-rolled hashing. No refresh tokens in this build.
- (decision) Actor identity for audit comes only from the authenticated principal through ICurrentUser, never from a request body field.
- (decision) The triage dashboard reads purpose-built query projections, never serialized aggregates. Attention signals - age, staleness, expiry proximity - are computed against a single injected TimeProvider so they are deterministic under test.
- (decision) Money is decimal with an explicit ISO-4217 currency code, never a floating point type. Identifiers are UUIDv7 generated in the Domain via Guid.CreateVersion7 so they sort monotonically and do not fragment indexes.
- (decision) Architecture is a modular monolith with ports and adapters and an inward-only dependency rule, enforced by project references: Domain and Application carry no EF Core, ASP.NET or Azure package references at all.
- (decision) The outbox dispatcher and integration event consumer run as BackgroundServices inside the API process for demo simplicity, but sit behind the same ports so extraction to an Azure Function is a hosting change rather than a rewrite.
- (assumption) Reviewers have a .NET 10 SDK available, or will accept installing it. Mitigated by stating the exact SDK requirement in the README and committing a global.json with rollForward set to latestMinor.
- (question) Does the candidate want an additional human-facing rendering of the architecture for the interview - an HTML walkthrough deck or a fuller solution-design document - beyond the terse spine? Deferred to the Finalize renderings step.
- (event) inputs captured: docs/inputs/challenge-brief.md consolidating the take-home prompt, the job description constraints, and the candidates stated intent
- (constraint) Local .NET SDK is 10.0.301, not the 10.0.302 named in the July 2026 release notes. global.json therefore pins a floor of 10.0.100 with rollForward latestMinor rather than an exact patch, which also makes the repo run on any 10.x SDK a reviewer happens to have.
- (decision) AD-1 enforcement asserts against the PROJECT FILES rather than the compiled assemblies. Roslyn omits assembly references that no code has used yet, so an assembly-level check passes vacuously on a thin project and cannot see a package that was added but not yet called. Corrected after the assembly-based version produced two false failures on empty projects.
- (event) Discovered two high-severity transitive advisories in the default template graph: Microsoft.OpenApi 2.0.0 via Microsoft.AspNetCore.OpenApi 10.0.10 (GHSA-v5pm-xwqc-g5wc) and SQLitePCLRaw.lib.e_sqlite3 2.1.11 via Microsoft.EntityFrameworkCore.Sqlite 10.0.10 (GHSA-2m69-gcr7-jv3q). Confirmed that bumping Microsoft.AspNetCore.OpenApi does not help - even 10.0.10 declares Microsoft.OpenApi 2.0.0.
- (decision) Central Package Management with CentralPackageTransitivePinningEnabled adopted, pinning Microsoft.OpenApi to 2.7.5 and SQLitePCLRaw.lib.e_sqlite3 to 2.1.12. Recorded as AD-12. CI builds with -warnaserror so NU1903 audit findings become build failures and an unpatched advisory cannot merge.
- (decision) Serilog sinks render with CultureInfo.InvariantCulture. Surfaced by analyzer CA1305 and fixed at the cause rather than suppressed: diagnostics that reformat numbers and dates per machine locale are harder to grep and differ between a laptop and a deployed host.
- (decision) Warnings are errors in CI only, not in local builds, so the edit loop stays fast while nothing warning-dirty can merge. Analyzer rules CA1707 and CA1859 are disabled for test projects only, via tests/Directory.Build.props, because underscored test names are the xUnit convention.
- (event) Foundation verified end to end: dotnet build -warnaserror clean with zero warnings, all 7 architecture tests green under xunit v3, and the API host returning 200 on /health, /openapi/v1.json and /scalar/v1 with Serilog request logs carrying TraceId - with no Azure resource present.
- (assumption) xunit v3 requires test projects to be console applications (OutputType Exe) rather than class libraries. Verified working: the dotnet new xunit template still scaffolds v2, so all three test projects were converted by hand.
- (event) create-vite 9.1.1 independently scaffolded TypeScript ~6.0.2, not 7.x, which corroborates the earlier decision to pin TypeScript 6.0.3 rather than the newer 7.0.2.
- (decision) Linting is oxlint 1.76, which the current Vite template ships in place of ESLint. This sidesteps the TypeScript 7 programmatic-API problem entirely, since oxlint does not depend on the TypeScript compiler API the way typescript-eslint does.
- (version) Frontend versions as actually resolved by npm, not merely requested: react and react-dom 19.2.8, react-router 8.3.0, vite 8.1.5, vitejs plugin-react 6.0.4, typescript 6.0.3, mantine 9.5.0, tanstack react-query 5.101.4, dayjs 1.11.21, oxlint 1.76.0. The Vite 8 with plugin-react 6 pairing flagged as a compatibility risk resolved with no peer conflicts.
- (decision) The client build emits into the API wwwroot and the API serves it with UseStaticFiles plus MapFallbackToFile, so a Release run is one process on one origin with no CORS configuration. In development the Vite dev server proxies /api and /health to the API instead, so the browser sees a single origin in both modes and no environment-dependent base URL is ever needed in client code.
- (decision) An MSBuild target chains npm ci and npm run build into Release builds only, never Debug. A reviewer can clone and run dotnet run -c Release without knowing anything about npm, while the development inner loop stays fast. wwwroot is gitignored because it is generated output.
- (decision) Mantine requires postcss-preset-mantine and postcss-simple-vars via postcss.config.cjs. Without that file components still compile but breakpoint mixins silently no-op, so it is load-bearing rather than optional.
- (event) Demo skeleton proven end to end in Release: a single process returns the SPA shell at / and at the client-side route /dashboard, while /health and /openapi/v1.json correctly take route priority as API endpoints. No cloud resource involved.
- (event) Reviewer gate: two of three parallel reviewers returned (rubric walker, version reality check). The adversarial reviewer produced no output file and was not rerun - the two that landed found enough substantive issues that a third pass was not the best use of remaining time.
- (constraint) react-router 8.x declares engines.node >=22.22.0. Verified empirically: npm install --dry-run --engine-strict fails EBADENGINE against the local 22.18.0. Held at 7.18.1, whose floor is >=20.0.0 and matches Vite 8s own Node floor.
- (decision) The react-router pin is about handoff, not local convenience: any reviewer on Node below 22.22 would hit the same EBADENGINE wall on a deliverable whose entire premise is clone-and-run. The v7 consolidated package exports the same BrowserRouter/Routes/Route/NavLink surface this app uses, so the constraint costs nothing. Re-verified: engine-strict passes and the bundle builds with no code change.
- (decision) Every test project declares xunit.runner.visualstudio alongside Microsoft.NET.Test.Sdk, and CI asserts a non-zero test count rather than trusting the exit code. xunit v3 runs under either VSTest or Microsoft Testing Platform, and a mismatched runner reports success while discovering nothing - a green build that tested zero code is worse than a red one.
- (decision) Guid.CreateVersion7 must be called with the injected clock, not the parameterless overload. UUIDv7 embeds a timestamp, so the parameterless form silently reintroduces wall-clock time and breaks the determinism the conventions table claims. FakeTimeProvider added via Microsoft.Extensions.TimeProvider.Testing 10.8.0 so there is something to test against.
- (decision) AD-2 gains a role axis: the transition table maps (status, action) to (resulting status, permitted roles) and exposes one function, QuoteTransitions.PermittedFor. Both the endpoint authorisation check and the AD-7 projection call it, and no Roles= string literal may appear on a transition endpoint. Without this, AD-7 promised an actor-aware action set that AD-2 had no actor dimension to supply, so the UI would offer actions the API answers with 403 - the same failure AD-7 exists to prevent, with a different status code.
- (decision) AD-13 names a closed role set - Admin, Requester, Reviewer, Vendor - and states the row-level read posture affirmatively: every authenticated user reads all data in this build, said out loud in the README and demo script. A stated scope decision reads as judgement; the same gap unmentioned reads as an oversight.
- (decision) AD-9 enumerates the complete anonymous set. A fallback authorisation policy applies to every endpoint and MapFallbackToFile is an endpoint, so login-and-health-only would have returned 401 for / and /login and the demo could not have started at all. Anonymous: login, health, the SPA fallback and static assets, the OpenAPI document, and Scalar. An integration test is the enforcement mechanism, not the prose.
- (decision) AD-6 narrowed from four dual-adapter ports to one. IIntegrationEventPublisher is the only genuine adapter pair; telemetry is an OTel exporter swap on the same pipeline; persistence is explicitly single-provider SQLite, which resolves a direct contradiction with the Deferred rows ban on a second EF provider. Verification restated to what can actually execute without a subscription: local adapter in CI, Azure adapter compile-verified behind Trait Requires=Azure, plus a configuration test asserting that connection-string presence selects the right adapter - the part that actually carries risk.
- (decision) AD-4s Prevents rewritten. It had claimed the outbox is what makes the audit trail trustworthy, but AD-5 establishes audit as a transactional projection that does not touch the outbox - the justification for the most expensive machinery in the spine did not describe what that machinery protects. Also: the interceptor writes all domain events to AuditEntry but only allow-listed ones to OutboxMessages, so the queue carries out-of-boundary integration events rather than an undifferentiated firehose.
- (decision) AD-15 extends concurrency control past Accept. AD-3 guarded the accept race alone, so two reviewers acting on one Submitted quote could both commit and the audit trail would faithfully record two transitions out of one source state - visibly wrong in the exact artifact built to prove correctness. SQLite has no rowversion, so an explicit int Version marked IsConcurrencyToken is named to stop a story author reaching for IsRowVersion and improvising.
- (decision) AD-16 makes migrations the only schema authority and bans EnsureCreated, which would silently skip migration history and never create AD-3s filtered unique index. The seed is treated as load-bearing rather than decoration: a triage dashboard over an empty database demonstrates nothing, so it produces one user per role, four organisations, and quotes in every lifecycle state including one near expiry and one request with competing quotes.
- (decision) AD-14 confines the SPA to one apiClient that is the only place a request originates or a token is attached, lint-enforced via no-restricted-globals and no-restricted-imports. Token lifetime 8 hours, held in memory with sessionStorage rehydration, XSS trade-off stated. With one client module per resource, every undecided auth question would otherwise have been re-decided in every module.
- (decision) Request.Status pinned to a closed set - Open, Awarded, Cancelled - changing only as a consequence of a quote transition in the same transaction, with no transition table of its own. It had been referenced by AD-3 but never defined, so FR-1 and FR-2 would each have invented a different enum. ER diagram now carries attributes for all four business entities, not just the two infrastructure tables.
- (decision) The list-query REQUEST contract is pinned as tightly as the response envelope: page 1-based, pageSize default 25 clamped at 100, sort direction by leading minus, closed per-endpoint sortable-field allow-list rejecting anything else 400. Several list surfaces get built by different stories and the generated-client-per-resource convention would harden each variant into its own module.
- (decision) A Delivery Order section now names three tiers so time pressure truncates the tail instead of gutting the middle: demo-critical (auth, entities, transitions with permittedActions, the accepted-quote invariant, audit, dashboard, seed), then differentiators (outbox and Service Bus, OTel, concurrency tokens), then reviewable artifacts (Docker, CI, README). Judged against the stated pass condition - correct but unbuildable in two days is a failure - the original mandate was not deliverable.
- (decision) Scope reductions taken to buy that time: AD-6 down to one adapter pair, Expire as a manual action plus a stored ExpiresAt that projections read (removing an entire hosted service and its idempotency questions while leaving expiry equally visible), and one log pipeline rather than two in parallel. All four of the candidates stated wants - Service Bus, logging, REST, login - survive intact.
- (event) The third reviewer (adversarial divergence) terminated with resource_exhausted and produced no findings. Not rerun: the two reviews that landed yielded roughly twenty substantive findings, all applied, and a third pass is not the best use of the remaining time before the demo.
- (decision) AD-3 now pins the PERSISTED representation of QuoteStatus as text with an explicit value converter, and the filtered index as HasFilter with SQLite double-quoted identifiers rather than the SQL Server bracket syntax in the EF docs. Both cautions had been raised by reviewers but existed only inside the review documents - not in the spine, which is what the build actually reads.
- (decision) The AD-3 database guarantee gets a test that bypasses the aggregate and attempts a second accepted row via raw SQL or a separate DbContext. This is the load-bearing part: if the enum maps to EFs default ordinal int, the filter matches zero rows forever, the DB-level invariant silently disappears, and every existing test still passes because the in-memory aggregate check catches the ordinary case first. An untested database constraint is decoration.
- (decision) Money is stored as INTEGER minor units plus a TEXT currency code, not as a decimal column. SQLite has no decimal type and EF falls back to TEXT, which is exact but sorts lexicographically - a quote list ordered by amount would put 900.00 above 1000.00. Minor units are exact and sort correctly, and a test asserts the ordering.
- (event) The AD-3 filtered unique index was mutation-tested rather than assumed: temporarily changing the migration filter from "Status" = 'Accepted' to an ordinal comparison made the guard test fail, then reverted. Confirms the test actually detects the silent-drift failure it was written for instead of passing vacuously.
- (decision) Analyzer rules are relaxed via an .editorconfig scoped to the Migrations folder only, with generated_code = true. EF regenerates that folder on every migrations add, so holding it to the hand-written bar is a fight that restarts constantly, and hand-editing a migration to satisfy an analyzer risks changing what it applies. The rest of Infrastructure keeps the full bar.
- (constraint) dotnet-ef 10.0.10 installed as a local tool with the manifest moved to the conventional .config/dotnet-tools.json. Reviewers run dotnet tool restore rather than needing a global install.
- (event) Port mismatch found and fixed: the template assigned the API 5016 while the Vite dev proxy targeted 5080. Nothing failed yet only because no endpoints existed; the first /api call from the dev server would have looked like a broken endpoint rather than a port mismatch.
- (event) Schema review with the candidate surfaced a real defect: only one foreign key existed (Quotes to Requests). Requests.ClientOrganizationId, Quotes.VendorOrganizationId and Users.OrganizationId were indexed but unconstrained, so the database would accept a quote from a vendor that does not exist. An index makes a lookup fast; it does not make a value valid.
- (decision) Surrogate key type is orthogonal to normal form - clarified explicitly because the two questions were tangled. UUIDv7 retained over INT IDENTITY for one concrete architectural reason: aggregates raise domain events before SaveChanges, and the audit and outbox rows written by the interceptor carry the subject id. A database-generated int is not available until after the INSERT, so those rows would reference 0 unless the save is split into two round trips, which would also stop the audit write being atomic with the change it describes. Secondary benefit: ids are not enumerable in URLs.
- (decision) QuoteStatus lookup table added, keyed by the status NAME rather than a surrogate int. An int key would force Quotes.Status to become an ordinal, and the AD-3 filtered index compares against the literal Accepted - so the index and the enum would need hand-alignment, which is the exact silent drift that index was already hardened against. Keying by name gives referential integrity, keeps the index working untouched, and leaves the database readable.
- (constraint) EF matches foreign keys on CLR type, not store type. A string-keyed lookup would not bind to the quote enum column even though both persist as identical TEXT. The lookup key is therefore typed as QuoteStatus with the same string conversion. Found by a design-time failure, not at runtime.
- (decision) Lookup rows are seeded via HasData in the migration rather than by the application seeder, so a database created by dotnet ef database update alone - app never started - already satisfies the Quotes.Status foreign key.
- (decision) RequestInvitations added as the genuine many-to-many the candidate was reaching for: composite primary key on (RequestId, VendorOrganizationId), so the database itself rejects a duplicate invitation. Its purpose is triage - without it a request with no quotes looks identical whether nobody was asked or nobody replied. Request.AwaitingResponseFrom exposes that signal.
- (event) SQLite foreign key enforcement verified rather than assumed. PRAGMA foreign_keys defaults to OFF in the engine, so declared constraints can be pure decoration while looking correct in the migration and in a database browser. Microsoft.Data.Sqlite turns it on; a test asserts the pragma is 1, and the four pre-existing index tests failed the moment the FKs landed because they had been inserting quotes from fabricated vendor ids - which was itself the proof.
- (decision) Delete behaviour is Restrict, not Cascade, for organisation references. Silently destroying a request and its whole audit history because someone tidied up an organisation is far worse than a failed delete.
- (decision) The seeder builds its graph through the aggregates - Request.Create, AddQuote, ApplyQuoteAction - rather than inserting rows. Direct inserts can fabricate states the transition table would refuse, and a demo over impossible data exercises a fiction. Side effect worth having: the seed is now a standing end-to-end test of the state machine that runs at every cold start, so a broken transition fails at start-up rather than mid-demo.
- (event) AD-17 came out of a test failure, not a design session. The seed test for quotes-near-expiry threw at query-compile time: EF Cores SQLite provider refuses to translate ordering comparisons on DateTimeOffset, because its default text form embeds the offset and one instant can be written two ways that sort differently. This would have blocked the dashboards central query - the whole point of FR-4 - and would have been found while building the UI with a day left.
- (decision) Fixed by a convention-wide value converter writing yyyy-MM-ddTHH:mm:ss.fffffffZ, where lexicographic order is chronological order. Applied via ConfigureConventions rather than per property so a later timestamp cannot reintroduce the unsortable form on one column. Fractional seconds padded to seven digits: without padding 12:00:00.5 sorts before 12:00:00.45, the same silent wrongness in a different disguise.
- (event) No migration accompanies AD-17, and the initially generated one was deleted after inspection. The column was TEXT before and after, only the encoding changed, and ef migrations has-pending-model-changes confirms the model still matches the snapshot - so the generated migration was an empty Up() implying a schema change that never happened. Keeping it would have been a false entry in the history.
- (constraint) Hand-written SQL must now emit the canonical timestamp format. The converter parses strictly and throws on anything else. Twelve literals across the two existing raw-SQL test files had to be corrected - which is the constraint proving itself: the strict parse surfaced them immediately instead of letting them compare wrong.
- (decision) Logging goes through source-generated LoggerMessage delegates rather than the ILogger extension methods, after CA1848 and CA1873 were raised as errors. CA1873 kept firing inside an IsEnabled guard until the computed arguments were hoisted into locals - the analyzer inspects the argument expression, not the surrounding guard.
- (event) Cold start verified end to end twice against a deleted database: first run applied all migrations and seeded 5 organisations, 6 users, 6 requests, 8 quotes; second run logged schema up to date and skipped the seed. Idempotency is asserted in tests, but the tests do not exercise the start-up path a reviewer will actually take.
- (decision) Authentication (AD-9, AD-10, AD-13 backend half, AD-14 backend half) implemented: ICurrentUser port and CurrentUser adapter over HttpContext.User, JWT bearer plus a deny-by-default fallback authorisation policy, POST /api/auth/login and GET /api/auth/me. Infrastructure gained a FrameworkReference to Microsoft.AspNetCore.App rather than the frozen Microsoft.AspNetCore.Http.Abstractions package, since Infrastructure targets Microsoft.NET.Sdk rather than Sdk.Web.
- (decision) The transition endpoint (AD-2, AD-7) implemented as POST /api/requests/{requestId}/quotes/{quoteId}/actions plus a companion GET, both projecting permittedActions via QuoteTransitions.PermittedFor with no role literal in the endpoint. AD-15's weak ETag/If-Match concurrency contract implemented alongside it. AD-8's exception-to-ProblemDetails mapping implemented as one IExceptionHandler ahead of the generic AddProblemDetails() handler, since no endpoint had needed it before now.
- (event) A real, pre-existing correctness bug was found and ruled out during this work, and a real test-harness bug was found and fixed. The suspect: DemoDataSeeder appeared to leave an untouched Submitted quote (hvacKestrel) persisted as UnderReview. Bisection - a pure in-memory domain repro, then a direct-seeder-with-SQL-logging repro - proved the domain and persistence layers both write and read this correctly in isolation. The actual cause: QuoteManagerApiFactory overrode the connection string via ConfigureAppConfiguration, which WebApplicationFactory does not reliably apply to the server CreateClient() builds against a minimal-hosting Program.cs (a bare Services access built a differently-configured host than CreateClient() did). Every "isolated" WebApplicationFactory test was therefore silently sharing and mutating one quotemanager.db left in the test project's bin directory, which is why a StartReview call from one test's run was still visible in a later, supposedly-fresh run. Fixed by switching to builder.UseSetting(...), which WebApplicationFactory itself uses internally and therefore applies regardless of which path builds the host.
- (event) A second, independent WebApplicationFactory issue surfaced once isolation was fixed and tests ran back-to-back: intermittent "The entry point exited without ever building an IHost" from Microsoft.Extensions.Hosting.HostFactoryResolver, caused by multiple WebApplicationFactory<Program> hosts starting concurrently under xunit's default parallelisation and racing on that resolver's shared static listener. Fixed with [assembly: CollectionBehavior(DisableTestParallelization = true)] on the integration test assembly; confirmed stable across three consecutive full-suite runs afterward.
- (decision) AD-5 audit implemented as AuditInterceptor : SaveChangesInterceptor, attached to every QuoteManagerDbContext, scanning ChangeTracker.Entries<AggregateRoot>() for pending domain events inside SavingChangesAsync so the AuditEntry inserts land in the same transaction as the change. Actor display name resolution had to cover a case that would otherwise silently misattribute rows: the seeder writes new AppUser entities and the events those users cause in one SaveChangesAsync call, so those users are not queryable from the database yet - the interceptor checks the change tracker for same-batch users before falling back to a database lookup.
- (event) src/QuoteManager.Web already existed (an earlier session's demo skeleton - Vite, React, Mantine, TanStack Query, react-router, oxlint - all at the versions this log already pinned), which a shallow directory search at the start of this session missed by checking for a folder literally named client rather than QuoteManager.Web. Confirmed before building anything further, by reading the existing files rather than re-scaffolding over them.
- (decision) AD-14 completed on top of that skeleton: apiClient.ts (sole fetch call site), authSession.ts (a plain, non-React module so apiClient can read/clear the session without importing a hook) plus AuthProvider.tsx wrapping it in React via useSyncExternalStore, and oxlint no-restricted-globals/no-restricted-imports with an override scoped to apiClient.ts itself so the single-egress rule is enforced by the build, not just written down.
- (event) Manual browser verification (not just automated tests) caught two real bugs that a first automated pass had missed entirely, because the failure conditions they exercise were not covered by any existing test:
  1. UseStaticFiles was positioned after UseAuthentication/UseAuthorization in Program.cs, so the deny-by-default fallback policy 401'd the SPA's own compiled JS/CSS bundle before the static-file middleware ever ran - invisible in the integration test suite because that environment has no compiled wwwroot to serve, so there was nothing for the bug to bite. Fixed by moving UseDefaultFiles/UseStaticFiles ahead of the auth middleware, which is also the conventional ASP.NET Core ordering.
  2. apiClient treated every 401 as "the current session expired," including the login endpoint's own 401 for a wrong password - masking the real auth.invalid_credentials problem-details message behind a generic "Your session has expired." Fixed by only triggering session invalidation when a session actually existed before the call.
  Both were found by actually typing a wrong password and watching real network traffic in a real browser, not by reasoning about the code - the concrete argument for the run skill's "verify in the browser" step over trusting a green test suite alone.
- (event) A Playwright e2e project was initially named QuoteManager.Web.E2E and silently `git status --porcelain` reported it as fully untracked with no way to stage it - `git check-ignore -v` traced this to a stock Visual Studio template rule, `*.e2e` (meant for VS trace files), matching the directory name case-insensitively on Windows. Renamed to QuoteManager.Web.E2ETests, which does not collide.
- (decision) User intervened before the dashboard was built to redirect the UI stack: shadcn/ui specifically, and a dark, soft, modern, aesthetically-pleasing look - a deliberate visual-identity call, made explicitly as the reason to pause before more screens got built on the old foundation. Mantine (and its PostCSS pipeline) removed outright rather than kept alongside; nothing about the swap touches the API, the auth flow, or apiClient - only the component layer and index.css changed.
- (decision) Tailwind CSS v4.3.3 plus shadcn/ui (CLI 4.16.0, `new-york` style, unified `radix-ui` package) is the UI foundation. Palette authored as OKLCH CSS custom properties in index.css rather than accepting a CLI preset's colours: background/card/popover/accent step up in lightness so depth reads from tone rather than borders, borders themselves are a soft 10% white wash. One theme only (`<html class="dark">` hard-coded), though the :root/.dark split leaves a future light mode as an additive change.
- (decision) Primary accent went through one iteration: shipped first as an indigo/violet (oklch 0.72 0.15 288), then the user asked to move off purple entirely, to blue or orange, judgement left to the assistant. Chose blue - specifically a clean azure/cobalt (oklch 0.68 0.16 232) - over orange because the background already carries a faint blue-violet tint (hue 264), so a blue primary stays in the same cool family and reads as harmonious rather than a complementary-colour clash; the lightness jump from background (0.15-0.21) to primary (0.68) supplies the pop on its own without needing a hue clash to do it. Applied to --primary, --sidebar-primary, --ring and --sidebar-ring in both :root and .dark; the low-chroma --accent hover surface was nudged from hue 270 to 264 to match the rest of the neutrals now that nothing is anchoring it to the old violet.
- (event) The shadcn CLI's `init` command hung on an interactive preset picker despite `-y` (a newer "starter kit" flow the flag does not skip) - avoided rather than debugged, by hand-authoring components.json and index.css and using only `shadcn add <component>` (which does not prompt once components.json exists) to vendor component source. Separately, `shadcn add` mis-resolved the `@/*` path alias and wrote files to a literal `./@/` directory: the root tsconfig.json is a solution-style file with no compilerOptions of its own, and the CLI evidently does not follow project references to find the alias in tsconfig.app.json. Fixed by adding the same `@/*` mapping directly to the root tsconfig.json. A related, separate discovery: `baseUrl` is deprecated as of this TypeScript version and errors under this project's strict settings, but `paths` alone (no `baseUrl`) resolves correctly without it.
- (event) Reconciled a naming drift before building the dashboard: the Consistency Conventions table names the transition route `POST /api/requests/{requestId}/quotes/{quoteId}/transitions`, but the already-shipped, already-tested endpoint used `/actions`. Renamed to `/transitions` (endpoint + the two test files referencing it) while the cost was still one line and three test strings, rather than let two "authoritative" spellings of the same thing coexist once more list endpoints started following the same table.
- (decision) AD-11 dashboard implemented: GET /api/dashboard (four triage buckets), GET /api/requests (list+detail), GET /api/organizations, sharing one PagedQuery/PagedResult<T> pair per the list-query convention. Discovered `[AsParameters] PagedQuery` sends every request into a 500 - Bad(HttpRequestException: "Required parameter int Page was not provided" - since minimal API's parameter-object binder treats non-nullable properties as required, not defaulted, which is exactly backwards for a clamp-don't-reject rule. Fixed by dropping AsParameters and having each endpoint accept `int? page, int? pageSize` directly, constructing PagedQuery in the handler.
- (event) Frontend dashboard/requests/organizations pages built on shadcn table/badge/skeleton, verified against the seed's known scenarios in a real browser (StartReview through to Accept/Reject/Return-to-submitted rendering correctly per role, dashboard buckets updating live after a transition) as well as new backend tests asserting the same scenarios by name rather than just 200 OK.
- (event) Manual per-role browser testing (not the existing test suite, which does not cover this) surfaced a real, unfixed authorisation gap: AD-13 states Vendor may only Withdraw/Submit quotes belonging to its own organisation, but QuoteTransitions.Resolve checks actorRoles only, and DomainActor carries no OrganizationId at all for anything to compare against. The current seed does not visibly expose this (the one case with two vendors' active quotes on one request has the second already past the states any Vendor action reaches), so it was found by reasoning through Request.cs's Guard method and confirmed by testing as a second vendor, not by a failing assertion. Recorded in AD-13's status as a known gap rather than fixed inline, since the fix reshapes DomainActor, which several call sites construct.
- (decision) AD-13 ownership gap closed. DomainActor now carries OrganizationId; vendor-gated transition rows check CanActForVendorOrganization; Request.AddQuote uses the same gate so creating a draft under a competitor's org is also refused. AppUser.ToActor and CurrentUser.ToActor thread the claim through, and every permittedActions projection passes the quote's VendorOrganizationId rather than roles alone. Integration test: vendor@ gets 403 and empty permittedActions when attempting Withdraw on vendor2@'s Submitted HVAC quote.
- (decision) Demo content retold for Warehouse Anywhere's actual business (researched from warehouseanywhere.com) rather than the generic HVAC/electrical/facilities scenario. "Client" organisations are now storage-need companies drawn from WA's stated verticals (pharma sample management, retail/CPG); "Vendor" organisations are the three service types WA actually brokers - a storage facility, a packing/crating partner, a freight/transportation carrier - rather than one undifferentiated supplier type. Every request title, description and quote note was rewritten to a real storage/packing/freight scenario while the underlying status/timing/who-wins structure was left untouched, so every seed-dependent test kept its meaning and only its literal strings changed. Demo emails moved from @quotemgr.test to @warehouseanywhere.test; UI copy (login, sidebar, header, dashboard section title) retitled to Warehouse Anywhere branding. No schema or domain-model change - this was purely content and copy, since Client/Vendor already modelled the two sides WA's platform actually connects.
- (decision) AD-8's validation half implemented: every request-body type moved to its own file under Api/Models (LoginRequest, ApplyQuoteActionRequest), each implementing IValidatableObject, wired via `builder.Services.AddValidation()` - .NET 10's minimal API validation, driven by a source-generated interceptor rather than a runtime reflection pass, which is why `QuoteManager.Api.csproj` also needed `InterceptorsNamespaces` extended with `Microsoft.AspNetCore.Http.Validation.Generated`. `Validate()` was deliberately not made to duplicate what an attribute already covers - `[Required]` on a string already rejects whitespace-only values in this runtime, so it earns no extra check - but it does close two real gaps an attribute cannot: `[EmailAddress]` matches on the presence of one interior `@` without anchoring to the string's edges, so a leading/trailing space passes it (LoginRequest.Validate catches it); and `JsonStringEnumConverter` still accepts a bare out-of-range integer on deserialisation, so an undefined QuoteAction would otherwise bind successfully and fall through every row of QuoteTransitions' table as an opaque 409 rather than a 400 naming the field (ApplyQuoteActionRequest.Validate catches it via Enum.IsDefined). FluentValidation was removed from every csproj and Directory.Packages.props - it had been referenced since the initial scaffold but no endpoint had ever used it, and a dead reference sitting next to the validation approach actually in force is exactly the kind of drift AD-8 itself exists to prevent. Verified by new integration tests in AnonymousEndpointsTests and QuoteTransitionTests asserting the 400 and the named field, not just that a request was rejected.
- (event) The previous pass left two endpoints on the pre-Models pattern: GetRequestsAsync and GetOrganizationsAsync still took `int? page, int? pageSize` as loose scalar parameters and hand-constructed `Common/PagedQuery`, because that type had been deliberately kept off `[AsParameters]` after an earlier bug (AD-11's status log: the binder 500'd, "Required parameter int Page was not provided"). Re-reading that bug: it was never about `[AsParameters]` itself, it was that `PagedQuery.Page`/`PageSize` were non-nullable `int` properties, which the parameter-object binder always treats as required regardless of what the constructor does with an absent value. GetDashboardAsync was also named as unconverted, but takes no client-supplied input at all - there is nothing to move.
- (decision) `PagedListQuery` added to `Api/Models`, replacing `Common/PagedQuery` outright (deleted, not deprecated - two competing pagination types would itself become the drift AD-8 exists to prevent). `Page`/`PageSize` are nullable so an absent query parameter binds to "no preference" rather than tripping the required-property rule; `ResolvedPage`/`ResolvedPageSize` carry the same defaulting and clamping the old constructor did. Bound via `[AsParameters]` on both `GetRequestsAsync` and `GetOrganizationsAsync` now that the actual cause is fixed, not worked around. `IValidatableObject.Validate()` on it is deliberately narrower than "reject anything user-supplied": it leaves the clamp-don't-reject rule for `pageSize` > 100 untouched (still silently capped, per the conventions table and the pre-existing `Requesting_a_page_size_over_the_cap_is_clamped_rather_than_rejected` test) and only rejects an explicit non-positive `page` or `pageSize` - a value clamping has no sensible interpretation for, which the old constructor used to silently reinterpret as "use the default," quietly hiding a caller bug rather than reporting it.
- (event) No `Api/Models` type was fabricated for `GetDashboardAsync` - it binds nothing from the caller, so there is nothing to validate. Confirmed no residual FluentValidation files, folders, or references remained anywhere in the repo beyond the package references removed in the prior entry - grepped clean.
- (decision) New coverage added rather than trusting the refactor: `RequestsEndpointTests.Requesting_a_non_positive_page_or_page_size_is_rejected_as_a_validation_problem`, and a new `OrganizationsEndpointTests` file (the organizations endpoint had no dedicated test file before this) covering the default-binding, clamp, and reject-on-negative-page cases for the same shared `PagedListQuery`.
- (event) Asked whether role-based endpoint security was possible "within current scope." Answer required distinguishing two different things that phrase can mean, because they are not the same fix: (1) a coarse `[Authorize(Roles=...)]`-style route gate - trivial, since the JWT already emits standard `ClaimTypes.Role` claims and needs no plumbing changes, but a static per-route role check cannot express AD-2's actual rule (which role may act depends on the quote's *current status*, not just the route), so it was already correctly rejected in favour of `QuoteTransitions` at the domain layer; and (2) the read-side gap AD-13's Deferred entry had already named affirmatively rather than left silent - `RequestEndpoints.GetRequestAsync` returned every quote's amount, notes, and vendor name, and every invitation, to any authenticated user regardless of role or organisation, so a Vendor viewing a request it was invited to could read a competitor's exact bid.
- (decision) User scoped the fix to (2), request-detail only, not the list endpoints or a broader route-gating pass. `RequestEndpoints.GetRequestAsync` now filters `Quotes`/`Invitations` before projecting, via a private `IsVendorOnlyView(actor)` check: true only when the actor has the `Vendor` role and none of `Admin`/`Reviewer`/`Requester`. Deliberately not implemented by reusing `DomainActor.CanActForVendorOrganization` - that method returns true for Admin (correct for gating a single vendor-owned action, wrong here since Admin must see every quote) and compares `actor.OrganizationId` against a *vendor* organisation id, which is meaningless for Reviewer/Requester whose `OrganizationId` is a *client* organisation (Reviewer's seeded account belongs to Palmetto, a client org) - reusing it verbatim would have silently zeroed out Reviewer's and Requester's visibility instead of Vendor's. `HasQuoted` on a filtered invitation is still computed against the full, unfiltered quote set, since a vendor is entitled to know whether *it* quoted regardless of who else did.
- (decision) List-level reads (`/api/requests`, `/api/organizations`, `/api/dashboard`) were explicitly left unfiltered per the scope chosen - they carry no competitor amount or identity (a list row's `QuoteCount` reveals a count, not who or how much), so the actual thing AD-13's Deferred entry warned about is closed without touching them. AD-13 and the Deferred table were reworded to state this precisely rather than as a single blanket "every authenticated user reads all data," which stopped being true the moment the fix landed.
- (event) Verified against the seed rather than assumed correct: `RequestsEndpointTests` gained two cases against the sample-storage request (SecureBase UnderReview, Crateworks Submitted, Interstate silently invited) - Crateworks (vendor2@) sees exactly its own quote and invitation; Interstate (vendor3@) sees zero quotes and only its own (unquoted) invitation. The pre-existing reviewer-sees-both-quotes test needed no change, which is itself the proof that the client-side roles were left untouched.
- (event) Audited the actual endpoint inventory against the brief's opening sentence ("A user can create and manage: Organizations, Requests, Quotes") and found a real, load-bearing gap invisible until named: every endpoint was GET or the one quote-transition POST. There was no way to create a Request, a Quote, or an Organisation through the app at all - every request/quote in the demo existed only because the seeder called the aggregate methods directly. `Request.Create`, `AddQuote`, `InviteVendor`, `Update`, `Cancel` were already fully modelled and unit-tested on the aggregate; the gap was entirely missing API wiring, not missing domain design. Scoped to Requests and Quotes (Organisation creation left out, since orgs are closer to reference data here and were not asked for).
- (decision) `POST /api/requests` and `POST /api/requests/{requestId}/quotes` added. Both follow the existing pattern exactly: an `Api/Models` type with `IValidatableObject`, no `[Authorize(Roles=...)]` at the endpoint, the domain method as the sole authority on legality and role. `CreateRequestRequest` validates `clientOrganizationId != Guid.Empty` (closes to a 400 naming the field rather than a 404 from a lookup that could never succeed) and the endpoint separately confirms the id resolves to a `Client`-kind organisation (400 `request.unknown_client_organization`) - a request raised against a vendor's own id would otherwise pass validation and only fail confusingly later. `CreateQuoteRequest` validates the vendor id, a non-negative amount, and a three-letter currency; ownership of the named vendor organisation is `Request.AddQuote`'s job, not this type's, per AD-13.
- (event) `Request.Create` took a `DomainActor` it never checked - the exact same shape of gap AD-13 found once already on the vendor/AddQuote side, just never exercised because the seeder was the only caller and always passed an Admin actor. Fixed the same way: `Request.Create` now throws `RequestCreationNotPermittedException` (403) unless the actor holds `Requester` or `Admin`. Confirmed every existing call site (seeder, `RequestAggregateTests`) already passes a permitted actor before landing the change, so nothing broke by tightening it.
- (decision) `RequestDetailResponse` gained `CanAddQuote` - AD-7's request-level counterpart to a quote's `permittedActions`, and the exact field AD-7's own Status note had predicted would "land with the dashboard/request-read work" once a request-response DTO existed. A boolean rather than a list, since exactly one request-level capability exists today: true only for the Vendor self-serve path (`IsEditable`, the actor holds Vendor with an organisation id, that organisation has no quote yet). Computed by one shared private helper used by both `GetRequestAsync` and the new `CreateRequestAsync`, so a freshly created request and a subsequently re-fetched one never disagree.
- (event) `POST /api/requests/{id}/quotes` failed its own new tests with `quote.concurrent_modification` on every attempt to actually create a quote (denial-path tests, which never reach `SaveChangesAsync`, passed fine) - a real EF Core gap, not a test-authoring mistake. Bisected with a throwaway direct-DbContext repro that caught `DbUpdateConcurrencyException.Entries`: the new `Quote` was tracked `Modified`, not `Added`. Root cause: the endpoint loads `Request` via a query (tracked, `Unchanged`) and calls `AddQuote`, which mutates the private `_quotes` backing field - the new child is only ever discovered through navigation fixup during `DetectChanges`, and because its key (UUIDv7) is already set in the constructor, EF's default heuristic assumes a reachable child with a non-default key on an already-tracked parent must be pre-existing rather than new, and issues an `UPDATE` that matches zero rows. This was invisible everywhere else in the codebase because the only other caller, `DemoDataSeeder`, always calls `context.Requests.AddRange(requests)` on the *top-level* aggregate, which cascades `Added` to every reachable child regardless of key state - so this exact scenario (load, then grow a collection, then save) had never actually been exercised before this endpoint existed. Fixed with one explicit `db.Quotes.Add(quote)` after the domain call; documented as a new Consistency Convention row so the next write path that grows a tracked aggregate's collection (`InviteVendor` has no endpoint yet, but will hit the identical issue the day one exists) does not rediscover this the same way.
- (event) Frontend: `RequestsListPage` gained a role-gated "New request" button routing to `/requests/new` (a form: title, client-organisation select sourced from `GET /api/organizations` filtered to `Kind === 'Client'`, optional description/needed-by); `RequestDetailPage` renders an `AddQuoteForm` card whenever `canAddQuote` is true, with the vendor organisation fixed to the caller's own (never a field the user fills in, so there is nothing to get wrong about whose quote it is). Both call through the one `apiClient` per AD-14; both role-gate the button/form as a UX courtesy only, since the API is the actual and only authority per AD-7 - a Vendor or Reviewer who reaches the request form by URL still gets a real 403, not a page that pretends the action does not exist.
- (event) Verified end-to-end in a real browser, not just the integration suite: logged in as `requester@`, created a request against Meridian Pharma Sampling, landed on its detail page with zero quotes and `isEditable: true`; logged in as `vendor@` (SecureBase), navigated to that same request by URL, saw the "Draft a quote" form (uninvited - `AddQuote` has never required an invitation, only ownership), submitted a $500 USD draft, and confirmed it appeared with status `Draft` and a `Submit` action, with the form itself now gone since SecureBase had a quote on the request.
- (decision) Full test suite (114 tests: 39 domain, 7 architecture, 68 integration) green after this work, including 2 new domain tests (`RequestAggregateTests`) and 15 new integration tests across `CreateRequestEndpointTests` and `CreateQuoteEndpointTests`.
- (event) Tier 1 complete, picked up Tier 2's outbox: AD-4 had a real `OutboxMessage` entity and EF configuration already scaffolded (index on `DispatchedAt`/`OccurredAt`, the works) with nothing writing to it and nothing reading from it — the entity existed, the mechanism did not. Two genuine gaps: nothing decided which domain events were integration-worthy, and nothing dispatched a row once written.
- (decision) The allow-list (AD-4's "one file in Application") is `IntegrationEventAllowList.Resolve(IDomainEvent) -> IIntegrationEvent?`, a single `switch` expression, not a config table or attribute — a `switch` over sealed record types is exhaustiveness-checkable by the compiler in a way a data-driven allow-list is not, and there are five entries, not fifty. `QuoteStatusChanged` (one type carrying every quote transition, per its own file's remarks) is pattern-matched on `{ To: QuoteStatus.Accepted }` rather than allow-listed by type, since only acceptance is genuinely integration-worthy among the transitions it carries. Five contracts result: `RequestCreated.v1`, `VendorInvited.v1`, `QuoteAccepted.v1`, `RequestAwarded.v1`, `RequestCancelled.v1` — each its own sealed record in `Application/Messaging/IntegrationEvents.cs`, never the Domain event type itself, so a Domain-internal rename cannot silently change a wire contract a consumer depends on.
- (decision) `AuditInterceptor` became `DomainEventPersistenceInterceptor` rather than gaining a sibling interceptor, and this was a considered choice, not the path of least resistance. AD-4's own text ("a SaveChangesAsync interceptor writes all of them to AuditEntry... and writes to OutboxMessages only those...") already names one interceptor doing both jobs. The alternative — a second, independently registered `OutboxInterceptor` reading the same `AggregateRoot.DomainEvents` before `AuditInterceptor` clears it — makes correctness depend on EF Core's interceptor invocation order, which is registration-order behaviour, not a documented contract. One interceptor reading once, delegating to `IntegrationEventAllowList` per event, then clearing once removes that dependency entirely rather than trusting it.
- (decision) The local default adapter (`InProcessIntegrationEventPublisher`) is a real `Channel<IntegrationEventEnvelope>`, not a publish call that returns `Task.CompletedTask` — a paired `InProcessIntegrationEventLogger` (`BackgroundService`) is the channel's one reader, so the demo's default path is a working local queue with something on the other end, standing in for whatever process would consume a real broker's messages. `ServiceBusIntegrationEventPublisher` stamps `ServiceBusMessage.MessageId` from the outbox row's own id specifically because AD-4's delivery guarantee is at-least-once — Service Bus duplicate-detection keys on exactly that field, so this line is what makes a redelivered message actually deduplicate on the broker side rather than merely being theoretically idempotent.
- (decision) `OutboxDispatcher` (`BackgroundService`, two-second poll, batch of 20, `OrderBy(m => m.Id)`) saves once per message inside its batch loop, not once per batch — considered and rejected the batch-level save, because a single `SaveChangesAsync` after N publishes means message 1's success and message N's failure share one transaction, and EF would roll back message 1's `DispatchedAt` write along with the failure that had nothing to do with it. Per-message save costs more round-trips at the volumes this dispatcher will ever see and buys correctness that would otherwise be silently wrong only under partial-batch failure — exactly the scenario a demo walkthrough would never exercise and a production incident would.
- (event) Selection (AD-6) had no test project that could reach `Infrastructure.DependencyInjection` in isolation — `QuoteManager.Api.IntegrationTests` goes through the full `WebApplicationFactory` pipeline, which is the wrong tool for "does this one DI branch resolve the right type." Added `QuoteManager.Infrastructure.Tests`, the first test project below the API tier, specifically because AD-6 demands a configuration test "executable without a subscription" and no existing project had the seam to write it small. `Microsoft.Extensions.Configuration.InMemory` does not exist as a separate package for this SDK line — `AddInMemoryCollection` has lived directly in `Microsoft.Extensions.Configuration` since .NET 8 — discovered via a live nuget.org lookup after a first `NU1101` rather than guessing at a plausible-sounding package id.
- (event) Verified against a real fresh-clone start-up, not just the new test project: deleted the demo database, ran `dotnet run`, and read the log in order — every one of the seed's 20 allow-listed events (6 `RequestCreated.v1`, 12 `VendorInvited.v1`, one `QuoteAccepted.v1`, one `RequestAwarded.v1`) is published and locally delivered within the same second the seed itself completes, and no `Organization*` contract or `RequestCancelled.v1` (the seed never cancels a request) appears anywhere in the log. This is the same seed `AuditTests` and now `OutboxWritingTests` assert against structurally; watching it happen in the actual startup log is what proves the dispatcher's polling loop, not just its query, is correct.
- (decision) Full test suite green after this work: 39 domain + 7 architecture + 70 integration (the seed's exact allow-listed counts, plus eventual-dispatch, in the two new `OutboxWritingTests`) + 4 in the new `QuoteManager.Infrastructure.Tests` (3 passing, 1 deliberately skipped — the Service Bus contract test, gated on `QUOTEMANAGER_SERVICEBUS_CONNECTION_STRING`, which is unset in every environment this ships to today).
- (decision) FR-4/FR-5's remaining gap closed: `GET /api/requests/{requestId}/activity` (`RequestActivityEndpoints`) reads `AuditEntry` directly, unioning rows whose `SubjectType` is the request itself or one of its quotes, ordered newest-first. Reused `RequestEndpoints.IsVendorOnlyView` (widened from `private` to `internal`) rather than re-deriving the same rule a second time: a pure Vendor viewer's timeline is filtered to only quote-subject rows for its own organisation's quotes, exactly matching what `GetRequestAsync` already lets that same viewer see in the quotes list. Request-subject events (`RequestCreated`, `VendorInvited`, `RequestAwarded`, `RequestCancelled`) name no vendor and carry no money, so they are always visible to anyone who can see the request at all.
- (event) The admin full-history test (`RequestActivityEndpointTests`) caught a real, previously-invisible ordering bug: asserting `RequestAwarded` sorts first among the trade-show request's history failed — `QuoteRejected` sorted ahead of it instead, and after a first attempted fix, `QuoteAccepted` did. Root cause: `DomainEventPersistenceInterceptor` minted every `AuditEntry`/`OutboxMessage` id from a fresh `Guid.CreateVersion7(timeProvider.GetUtcNow())` call per domain event, but `Request.ApplyQuoteAction(Accept, ...)` raises an Accept, a Reject per superseded sibling, and a `RequestAwarded` from one call sharing one `DateTimeOffset` argument — three ids minted microseconds apart in real wall-clock time. `Guid.CreateVersion7` only encodes millisecond resolution (RFC 9562's 48-bit `unix_ts_ms`), so three calls landing in the same millisecond do not actually sort by call order — they tie on the timestamp field and fall back to that call's random bits, which is why the first fix attempt (advancing by `DateTimeOffset.AddTicks`, i.e. 100ns units) changed nothing observable: ticks below 1ms never touch the field the sort depends on. This is the same latent risk to `OutboxDispatcher`'s `OrderBy(m => m.Id)` insertion-order guarantee that AD-4's own text asserts holds "for free" from UUIDv7 — true only across calls far enough apart to land in different milliseconds, never guaranteed for events raised together in one `SaveChangesAsync`. Fixed at the source rather than in the query: the interceptor now captures one `idClock = timeProvider.GetUtcNow()` per save and mints every id in that call as `idClock.AddMilliseconds(offset++)`, guaranteeing strictly increasing ids for everything raised in the same transaction regardless of real clock resolution. Full suite (124 tests) green after the fix, including the newly-passing ordering assertion.
- (event) Frontend: `RequestDetailPage` gained an "Activity" section rendering `ActivityTimeline`, a simple vertical dot-and-line list sourced from the new endpoint (`getRequestActivity`, `pageSize=100` since a single request's history is small and a second page would be an odd thing to hide behind a click here). Deliberately renders exactly what the endpoint returns with no client-side role logic — the filtering already happened server-side per AD-13, so the component has nothing to get wrong.
- (event) A browser-driven test of the create-request/create-quote flow surfaced a real validation gap (the automation's own $0.00 result was itself an artifact of a CDP workaround not triggering React state, but the underlying question it raised — "should a $0 quote be accepted at all?" — was legitimate): `CreateQuoteRequest.Validate` rejected `Amount < 0` but let `Amount == 0` through, and `AddQuoteForm`'s `canSubmit`/`min` matched it exactly. A $0.00 storage/packing/freight quote is never a real offer. Traced the gap to its actual root rather than patching the API model alone: `Money`'s own constructor — the one value object every caller of `Quote.Draft`/`Quote.ApplyEdit` shares — had the identical `amount < 0` check, so the API's rule was a UX-friendly restatement of a domain invariant that was itself too permissive. Tightened both to `amount <= 0` in the same pass (API model for the named 400, `Money` for defense in depth per the architecture's stated principle), plus the frontend form's `min`/`canSubmit`. Added `MoneyTests` (new file — no domain-level Money test had existed before) and a zero-amount case to `CreateQuoteEndpointTests`. Full suite (127 tests) green.
